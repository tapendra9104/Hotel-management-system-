These SVG files are local asset placeholders for the homepage image system.

They exist because the chat-uploaded source photos were not available as files in
the workspace during implementation. The homepage now points only to local
assets under `images/`, so you can replace any placeholder by dropping the final
photo into this folder and updating the matching entry in `../scripts/images-config.js`.
